"name": "pi-chess-app",
  "private": true,
  "workspaces": ["server", "client", "pi-integration"],
  "scripts": {
    "dev": "concurrently -k \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
